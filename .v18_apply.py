from pathlib import Path


def replace_once(path: str, old: str, new: str):
    p = Path(path)
    text = p.read_text(encoding="utf-8")
    if old not in text:
        raise SystemExit(f"Expected text not found in {path}: {old[:160]!r}")
    p.write_text(text.replace(old, new, 1), encoding="utf-8")

# --- local-db model ---------------------------------------------------------
replace_once(
    "app/local-db.ts",
    'export type Entry = { id: number; personId: number; personName: string; kind: string; direction: string; title: string; amount: number; dueDate: string | null; status: string; note: string; createdAt: string; receipt: ReceiptAttachment | null };',
    'export type Entry = { id: number; personId: number; personName: string; kind: string; direction: string; title: string; amount: number; dueDate: string | null; status: string; note: string; createdAt: string; receipt: ReceiptAttachment | null; role: "obligation" | "settlement"; settlesEntryId: number | null; settledByEntryId: number | null; transactionDate: string | null };',
)
replace_once(
    "app/local-db.ts",
    '  status?: string;\n};',
    '  status?: string;\n  entryRole?: "obligation" | "settlement";\n};',
)
replace_once(
    "app/local-db.ts",
    'type StoredEntry = Omit<Entry, "id" | "personName" | "receipt"> & { id?: number; receipt?: ReceiptAttachment | null };',
    'type StoredEntry = Omit<Entry, "id" | "personName" | "receipt" | "role" | "settlesEntryId" | "settledByEntryId" | "transactionDate"> & { id?: number; role?: "obligation" | "settlement"; settlesEntryId?: number | null; transactionDate?: string | null; receipt?: ReceiptAttachment | null };',
)

old_accounts = '''    const directEntries = entries.filter((entry) => entry.personId === person.id);\n    const directBalance = directEntries\n      .filter((entry) => entry.status === "open")\n      .reduce((sum, entry) => sum + (entry.direction === "receivable" ? entry.amount : -entry.amount), 0);\n    const items: PersonLedgerItem[] = directEntries.map((entry) => ({\n      id: `entry-${entry.id}`,\n      source: "entry",\n      sourceId: entry.id,\n      title: entry.title,\n      detail: `${entry.direction === "receivable" ? "طلب مستقیم" : "بدهی مستقیم"}${entry.dueDate ? ` • سررسید ${entry.dueDate}` : ""}`,\n      date: entry.createdAt.slice(0, 10),\n      amount: entry.amount,\n      effect: entry.status === "open" ? (entry.direction === "receivable" ? entry.amount : -entry.amount) : 0,\n      status: entry.status,\n    }));'''
new_accounts = '''    const directEntries = entries.filter((entry) => entry.personId === person.id);\n    const directBalance = directEntries\n      .filter((entry) => entry.role === "obligation" && entry.status === "open")\n      .reduce((sum, entry) => sum + (entry.direction === "receivable" ? entry.amount : -entry.amount), 0);\n    const entryMap = new Map(entries.map((entry) => [entry.id, entry]));\n    const items: PersonLedgerItem[] = directEntries.map((entry) => {\n      const settlement = entry.settledByEntryId ? entryMap.get(entry.settledByEntryId) : undefined;\n      const source = entry.settlesEntryId ? entryMap.get(entry.settlesEntryId) : undefined;\n      const isSettlement = entry.role === "settlement";\n      return {\n        id: `entry-${entry.id}`,\n        source: "entry",\n        sourceId: entry.id,\n        title: entry.title,\n        detail: isSettlement\n          ? `${entry.direction === "receivable" ? "دریافت تسویه" : "پرداخت تسویه"}${source ? ` • برای «${source.title}»` : ""}`\n          : `${entry.direction === "receivable" ? "طلب مستقیم" : "بدهی مستقیم"}${entry.dueDate ? ` • سررسید ${entry.dueDate}` : ""}${settlement ? ` • تسویه با تراکنش #${settlement.id}` : ""}`,\n        date: entry.transactionDate ?? entry.createdAt.slice(0, 10),\n        amount: entry.amount,\n        effect: isSettlement\n          ? (entry.direction === "receivable" ? entry.amount : -entry.amount)\n          : entry.status === "open" ? (entry.direction === "receivable" ? entry.amount : -entry.amount) : 0,\n        status: isSettlement ? "settlement" : entry.status,\n        entryRole: entry.role,\n      };\n    });'''
replace_once("app/local-db.ts", old_accounts, new_accounts)

old_entries_map = '''  const entries = storedEntries\n    .map((entry) => ({ ...entry, personName: names.get(entry.personId) ?? "نامشخص", receipt: entry.receipt ?? null }))\n    .sort((a, b) => b.createdAt.localeCompare(a.createdAt) || b.id - a.id);'''
new_entries_map = '''  const settlementBySource = new Map<number, number>();\n  for (const entry of storedEntries) {\n    if (entry.settlesEntryId) settlementBySource.set(entry.settlesEntryId, entry.id);\n  }\n  const entries: Entry[] = storedEntries\n    .map((entry) => {\n      const role: Entry["role"] = entry.role === "settlement" || entry.settlesEntryId ? "settlement" : "obligation";\n      return {\n        ...entry,\n        personName: names.get(entry.personId) ?? "نامشخص",\n        receipt: entry.receipt ?? null,\n        role,\n        settlesEntryId: entry.settlesEntryId ?? null,\n        settledByEntryId: role === "obligation" ? settlementBySource.get(entry.id) ?? null : null,\n        transactionDate: entry.transactionDate ?? null,\n      };\n    })\n    .sort((a, b) => (b.transactionDate ?? b.createdAt.slice(0, 10)).localeCompare(a.transactionDate ?? a.createdAt.slice(0, 10)) || b.id - a.id);'''
replace_once("app/local-db.ts", old_entries_map, new_entries_map)

old_add_update = '''    if (id) {\n      const current = await requestResult(store.get(id)) as StoredEntry | undefined;\n      if (!current) throw new Error("ثبت موردنظر پیدا نشد.");\n      store.put({ ...current, id, personId, kind, direction, title, amount, dueDate: cleanText(payload.dueDate, 10) || null, note: cleanText(payload.note, 400), receipt: receipt ?? current.receipt ?? null });\n    } else {\n      store.add({ personId, kind, direction, title, amount, dueDate: cleanText(payload.dueDate, 10) || null, status: "open", note: cleanText(payload.note, 400), createdAt: new Date().toISOString(), receipt } satisfies StoredEntry);\n    }'''
new_add_update = '''    if (id) {\n      const current = await requestResult(store.get(id)) as StoredEntry | undefined;\n      if (!current) throw new Error("ثبت موردنظر پیدا نشد.");\n      if (current.role === "settlement" || current.settlesEntryId) throw new Error("تراکنش تسویه از صفحه تراکنش اصلی مدیریت می‌شود و قابل تبدیل به بدهی/طلب نیست.");\n      store.put({ ...current, id, personId, kind, direction, title, amount, dueDate: cleanText(payload.dueDate, 10) || null, note: cleanText(payload.note, 400), receipt: receipt ?? current.receipt ?? null, role: "obligation", settlesEntryId: null, transactionDate: current.transactionDate ?? null });\n    } else {\n      store.add({ personId, kind, direction, title, amount, dueDate: cleanText(payload.dueDate, 10) || null, status: "open", note: cleanText(payload.note, 400), createdAt: new Date().toISOString(), receipt, role: "obligation", settlesEntryId: null, transactionDate: null } satisfies StoredEntry);\n    }'''
replace_once("app/local-db.ts", old_add_update, new_add_update)

old_toggle_delete = '''  if (operation === "toggle_entry") {\n    const id = positiveInteger(payload.id, "شناسه");\n    const transaction = db.transaction("entries", "readwrite");\n    const store = transaction.objectStore("entries");\n    const entry = await requestResult(store.get(id)) as StoredEntry | undefined;\n    if (!entry) throw new Error("ثبت موردنظر پیدا نشد.");\n    store.put({ ...entry, id, status: entry.status === "open" ? "paid" : "open" });\n    await transactionDone(transaction);\n    return;\n  }\n\n  if (operation === "delete_entry") {\n    const id = positiveInteger(payload.id, "شناسه");\n    const transaction = db.transaction("entries", "readwrite");\n    transaction.objectStore("entries").delete(id);\n    await transactionDone(transaction);\n    return;\n  }'''
new_toggle_delete = '''  if (operation === "settle_entry" || operation === "toggle_entry") {\n    const id = positiveInteger(payload.id, "شناسه");\n    const entries = await all<StoredEntry & { id: number }>(db, "entries");\n    const entry = entries.find((item) => item.id === id);\n    if (!entry) throw new Error("ثبت موردنظر پیدا نشد.");\n    if (entry.role === "settlement" || entry.settlesEntryId) throw new Error("خود تراکنش تسویه دوباره تسویه نمی‌شود.");\n\n    const linkedSettlements = entries.filter((item) => item.settlesEntryId === id);\n    if (operation === "toggle_entry" && entry.status !== "open") {\n      const transaction = db.transaction("entries", "readwrite");\n      const store = transaction.objectStore("entries");\n      for (const settlement of linkedSettlements) store.delete(settlement.id);\n      store.put({ ...entry, id, status: "open", role: entry.role ?? "obligation", settlesEntryId: null, transactionDate: entry.transactionDate ?? null });\n      await transactionDone(transaction);\n      return;\n    }\n\n    if (entry.status !== "open") throw new Error("این بدهی/طلب قبلاً تسویه شده است.");\n    if (linkedSettlements.length) throw new Error("برای این ثبت قبلاً تراکنش تسویه ساخته شده است.");\n    const settlementDate = cleanText(payload.settlementDate, 10) ? cleanIsoDate(payload.settlementDate, "تاریخ تسویه") : currentLocalIsoDate();\n    const receipt = payload.receipt ? cleanReceipt(payload.receipt) : null;\n    const now = new Date().toISOString();\n    const transaction = db.transaction("entries", "readwrite");\n    const store = transaction.objectStore("entries");\n    store.put({ ...entry, id, status: "paid", role: entry.role ?? "obligation", settlesEntryId: null, transactionDate: entry.transactionDate ?? null });\n    store.add({\n      personId: entry.personId,\n      kind: "settlement",\n      direction: entry.direction,\n      title: `${entry.direction === "receivable" ? "دریافت" : "پرداخت"} بابت «${entry.title}»`,\n      amount: entry.amount,\n      dueDate: null,\n      status: "paid",\n      note: cleanText(payload.note, 400),\n      createdAt: now,\n      receipt,\n      role: "settlement",\n      settlesEntryId: id,\n      transactionDate: settlementDate,\n    } satisfies StoredEntry);\n    await transactionDone(transaction);\n    return;\n  }\n\n  if (operation === "reopen_entry") {\n    const id = positiveInteger(payload.id, "شناسه");\n    const entries = await all<StoredEntry & { id: number }>(db, "entries");\n    const entry = entries.find((item) => item.id === id);\n    if (!entry) throw new Error("ثبت موردنظر پیدا نشد.");\n    if (entry.role === "settlement" || entry.settlesEntryId) throw new Error("برای بازگردانی، تراکنش اصلی بدهی/طلب را باز کن.");\n    const transaction = db.transaction("entries", "readwrite");\n    const store = transaction.objectStore("entries");\n    for (const settlement of entries.filter((item) => item.settlesEntryId === id)) store.delete(settlement.id);\n    store.put({ ...entry, id, status: "open", role: entry.role ?? "obligation", settlesEntryId: null, transactionDate: entry.transactionDate ?? null });\n    await transactionDone(transaction);\n    return;\n  }\n\n  if (operation === "delete_entry") {\n    const id = positiveInteger(payload.id, "شناسه");\n    const entries = await all<StoredEntry & { id: number }>(db, "entries");\n    const entry = entries.find((item) => item.id === id);\n    if (!entry) return;\n    const transaction = db.transaction("entries", "readwrite");\n    const store = transaction.objectStore("entries");\n    if (entry.role === "settlement" || entry.settlesEntryId) {\n      const sourceId = entry.settlesEntryId;\n      if (sourceId) {\n        const source = entries.find((item) => item.id === sourceId);\n        if (source) store.put({ ...source, id: sourceId, status: "open", role: source.role ?? "obligation", settlesEntryId: null, transactionDate: source.transactionDate ?? null });\n      }\n      store.delete(id);\n    } else {\n      for (const settlement of entries.filter((item) => item.settlesEntryId === id)) store.delete(settlement.id);\n      store.delete(id);\n    }\n    await transactionDone(transaction);\n    return;\n  }'''
replace_once("app/local-db.ts", old_toggle_delete, new_toggle_delete)

# --- FinanceApp -------------------------------------------------------------
replace_once(
    "app/FinanceApp.tsx",
    'type Sheet = "actions" | "person" | "entry" | "group" | "expense" | "settlement" | "person-ledger" | "loan-form" | "loan-payment" | "check-form" | "check-transfer" | "transaction-detail" | "tools" | null;',
    'type Sheet = "actions" | "person" | "entry" | "entry-settlement" | "group" | "expense" | "settlement" | "person-ledger" | "loan-form" | "loan-payment" | "check-form" | "check-transfer" | "transaction-detail" | "tools" | null;',
)
replace_once(
    "app/FinanceApp.tsx",
    '  const [editingEntryId, setEditingEntryId] = useState<number | null>(null);',
    '  const [editingEntryId, setEditingEntryId] = useState<number | null>(null);\n  const [settlingEntryId, setSettlingEntryId] = useState<number | null>(null);',
)
replace_once(
    "app/FinanceApp.tsx",
    '  const editingEntry = data?.entries.find((entry) => entry.id === editingEntryId);',
    '  const editingEntry = data?.entries.find((entry) => entry.id === editingEntryId);\n  const settlingEntry = data?.entries.find((entry) => entry.id === settlingEntryId);',
)
replace_once(
    "app/FinanceApp.tsx",
    '    ...(data?.entries ?? []).filter((entry) => entry.dueDate).map((entry) => ({',
    '    ...(data?.entries ?? []).filter((entry) => entry.role === "obligation" && entry.dueDate).map((entry) => ({',
)

anchor = '''  function openEntry(kind: string, entry?: Entry) {\n    setEditingEntryId(entry?.id ?? null);\n    setEntryKind(entry?.kind ?? kind);\n    setSheet("entry");\n  }'''
replacement = anchor + '''\n\n  function handleEntrySettlement(entry: Entry) {\n    if (entry.role === "settlement") return;\n    if (entry.status === "open") {\n      setSettlingEntryId(entry.id);\n      setSheet("entry-settlement");\n      return;\n    }\n    const suffix = entry.settledByEntryId ? " تراکنش تسویه مرتبط هم حذف می‌شود." : "";\n    if (window.confirm(`«${entry.title}» دوباره باز شود؟${suffix}`)) void post({ operation: "reopen_entry", id: entry.id }, { close: false });\n  }'''
replace_once("app/FinanceApp.tsx", anchor, replacement)

replace_once(
    "app/FinanceApp.tsx",
    '''  function deleteEntry(entry: Entry) {\n    if (window.confirm(`ثبت «${entry.title}» حذف شود؟`)) void post({ operation: "delete_entry", id: entry.id }, { close: false });\n  }''',
    '''  function deleteEntry(entry: Entry) {\n    const message = entry.role === "settlement"\n      ? `تراکنش «${entry.title}» حذف شود؟ بدهی/طلب اصلی دوباره باز خواهد شد.`\n      : `ثبت «${entry.title}» حذف شود؟${entry.settledByEntryId ? " تراکنش تسویه مرتبط هم حذف می‌شود." : ""}`;\n    if (window.confirm(message)) void post({ operation: "delete_entry", id: entry.id }, { close: false });\n  }''',
)

insert_after_submit = '''  async function submitEntry(event: FormEvent<HTMLFormElement>) {\n    event.preventDefault();\n    const form = new FormData(event.currentTarget);\n    let dueDate: string | null;\n    let attachment: ReceiptAttachment | null;\n    try {\n      dueDate = jalaliInputToIso(String(form.get("dueDate") ?? ""));\n      attachment = await attachmentFromFormData(form);\n    } catch (formError) {\n      setError(formError instanceof Error ? formError.message : "اطلاعات ثبت معتبر نیست.");\n      return;\n    }\n    await post({\n      operation: editingEntry ? "update_entry" : "add_entry",\n      id: editingEntry?.id,\n      personId: form.get("personId"),\n      kind: entryKind,\n      direction: entryKind === "receivable" ? "receivable" : entryKind === "debt" ? "debt" : form.get("direction"),\n      title: form.get("title"), amount: form.get("amount"), dueDate, note: form.get("note"), receipt: attachment,\n    });\n  }'''
new_submit = insert_after_submit + '''\n\n  async function submitDirectEntrySettlement(event: FormEvent<HTMLFormElement>) {\n    event.preventDefault();\n    if (!settlingEntry) return;\n    const form = new FormData(event.currentTarget);\n    let settlementDate: string;\n    let receipt: ReceiptAttachment | null;\n    try {\n      settlementDate = jalaliInputToIso(String(form.get("settlementDate") ?? ""), true) as string;\n      receipt = await attachmentFromFormData(form);\n    } catch (formError) {\n      setError(formError instanceof Error ? formError.message : "اطلاعات تسویه معتبر نیست.");\n      return;\n    }\n    await post({ operation: "settle_entry", id: settlingEntry.id, settlementDate, note: form.get("note"), receipt });\n    setSettlingEntryId(null);\n  }'''
replace_once("app/FinanceApp.tsx", insert_after_submit, new_submit)

# replace all direct toggle posts in JSX with semantic handler
text = Path("app/FinanceApp.tsx").read_text(encoding="utf-8")
text = text.replace('void post({ operation: "toggle_entry", id: item.entry.id }, { close: false })', 'handleEntrySettlement(item.entry)')
text = text.replace('void post({ operation: "toggle_entry", id: entry.id }, { close: false })', 'handleEntrySettlement(entry)')
text = text.replace('onToggleEntry={(entry) => void post({ operation: "toggle_entry", id: entry.id }, { close: false })}', 'onToggleEntry={handleEntrySettlement}')
Path("app/FinanceApp.tsx").write_text(text, encoding="utf-8")

replace_once(
    "app/FinanceApp.tsx",
    '          {sheet === "entry" && <EntryForm key={editingEntry?.id ?? `new-${entryKind}`} kind={entryKind} people={people} initialEntry={editingEntry} onNeedPerson={() => setSheet("person")} onSubmit={submitEntry} busy={busy} />}',
    '          {sheet === "entry" && <EntryForm key={editingEntry?.id ?? `new-${entryKind}`} kind={entryKind} people={people} initialEntry={editingEntry} onNeedPerson={() => setSheet("person")} onSubmit={submitEntry} busy={busy} />}\n          {sheet === "entry-settlement" && settlingEntry && <DirectEntrySettlementForm entry={settlingEntry} onSubmit={submitDirectEntrySettlement} busy={busy} />}',
)

old_entryrow = '''function EntryRow({ entry, onToggle, onEdit, onDelete, onOpen, compact = false }: { entry: Entry; onToggle: () => void; onEdit: () => void; onDelete: () => void; onOpen: () => void; compact?: boolean }) {\n  const meta = entryMeta[entry.kind] ?? entryMeta.debt;\n  return <article className={`entry-row ${entry.status === "paid" ? "paid" : ""} ${compact ? "compact" : ""}`}>\n    <span className={`entry-icon ${meta.tone}`}><Icon name={meta.icon} /></span>\n    <button className="entry-copy entry-open-copy" onClick={onOpen}><strong>{entry.title}</strong><small>{entry.personName}{entry.dueDate ? ` • ${persianDate(entry.dueDate)}` : ""}{entry.receipt ? " • پیوست دارد" : ""}</small></button>\n    <div className="entry-amount"><strong className={entry.direction === "receivable" ? "text-green" : "text-coral"}>{entry.direction === "receivable" ? "+" : "−"}{shortMoney(entry.amount)}</strong><div className="entry-actions"><button className="detail-link" onClick={onOpen}>جزئیات</button><button onClick={onToggle}>{entry.status === "paid" ? "بازگردانی" : "تسویه"}</button><button onClick={onEdit} aria-label="ویرایش"><Icon name="edit" size={13} /></button><button onClick={onDelete} aria-label="حذف"><Icon name="trash" size={13} /></button></div></div>\n  </article>;\n}'''
new_entryrow = '''function EntryRow({ entry, onToggle, onEdit, onDelete, onOpen, compact = false }: { entry: Entry; onToggle: () => void; onEdit: () => void; onDelete: () => void; onOpen: () => void; compact?: boolean }) {\n  const isSettlement = entry.role === "settlement";\n  const meta = isSettlement ? { label: "تسویه", icon: "settlement" as IconName, tone: entry.direction === "receivable" ? "green" : "coral" } : (entryMeta[entry.kind] ?? entryMeta.debt);\n  return <article className={`entry-row ${!isSettlement && entry.status === "paid" ? "paid" : ""} ${isSettlement ? "settlement-entry" : ""} ${compact ? "compact" : ""}`}>\n    <span className={`entry-icon ${meta.tone}`}><Icon name={meta.icon} /></span>\n    <button className="entry-copy entry-open-copy" onClick={onOpen}><strong>{entry.title}</strong><small>{entry.personName}{isSettlement && entry.transactionDate ? ` • ${persianDate(entry.transactionDate)}` : entry.dueDate ? ` • ${persianDate(entry.dueDate)}` : ""}{isSettlement ? " • تراکنش تسویه" : ""}{entry.receipt ? " • پیوست دارد" : ""}</small></button>\n    <div className="entry-amount"><strong className={entry.direction === "receivable" ? "text-green" : "text-coral"}>{entry.direction === "receivable" ? "+" : "−"}{shortMoney(entry.amount)}</strong><div className="entry-actions"><button className="detail-link" onClick={onOpen}>جزئیات</button>{!isSettlement && <button onClick={onToggle}>{entry.status === "paid" ? "بازگردانی" : "تسویه"}</button>}{!isSettlement && entry.status === "open" && <button onClick={onEdit} aria-label="ویرایش"><Icon name="edit" size={13} /></button>}<button onClick={onDelete} aria-label="حذف"><Icon name="trash" size={13} /></button></div></div>\n  </article>;\n}'''
replace_once("app/FinanceApp.tsx", old_entryrow, new_entryrow)

# Direct settlement form before group settlement form
settlement_anchor = 'function SettlementForm({ group, draft, onSubmit, busy }: { group?: Group; draft: SettlementDraft; onSubmit: (event: FormEvent<HTMLFormElement>) => void; busy: boolean }) {'
direct_form = '''function DirectEntrySettlementForm({ entry, onSubmit, busy }: { entry: Entry; onSubmit: (event: FormEvent<HTMLFormElement>) => void; busy: boolean }) {\n  return <form onSubmit={onSubmit}><div className="sheet-title"><p>این پرداخت/دریافت به‌عنوان یک تراکنش مستقل در تاریخچه می‌ماند</p><h2>تسویه • {entry.title}</h2></div>\n    <div className="settlement-source-summary"><small>{entry.direction === "receivable" ? "مبلغی که دریافت می‌کنی" : "مبلغی که پرداخت می‌کنی"}</small><strong>{money(entry.amount)}</strong><span>{entry.personName}</span></div>\n    <JalaliDatePicker name="settlementDate" label="تاریخ تسویه شمسی" defaultToday required />\n    <label>یادداشت <small>(اختیاری)</small><textarea name="note" rows={2} placeholder="مثلاً کارت‌به‌کارت یا نقدی" /></label>\n    <AttachmentField label="پیوست / مدرک تسویه" />\n    <p className="form-hint">بعد از ثبت، بدهی/طلب اصلی بسته می‌شود اما حذف نمی‌شود؛ یک تراکنش متناظر «{entry.direction === "receivable" ? "دریافت" : "پرداخت"}» هم به آن لینک می‌شود.</p>\n    <SubmitButton busy={busy} label="ثبت تراکنش تسویه" />\n  </form>;\n}\n\n'''
replace_once("app/FinanceApp.tsx", settlement_anchor, direct_form + settlement_anchor)

# Person ledger rendering: keep settlement transaction visible as a real row
old_person_list = '<div className="person-ledger-list">{account.items.map((item) => <article key={item.id} className={item.status === "paid" ? "ledger-item paid" : "ledger-item"}><span className={`ledger-source ${item.source}`}><Icon name={item.source === "entry" ? "book" : item.source === "check" ? "check" : item.source === "expense" ? "receipt" : "settlement"} size={15} /></span><button className="ledger-item-open" onClick={() => onOpenItem(item)}><strong>{item.title}</strong><small>{item.detail} • {persianDate(item.date)}</small></button><b className={item.effect >= 0 ? "text-green" : "text-coral"}>{item.status === "paid" ? "تسویه‌شده" : `${item.effect >= 0 ? "+" : "−"}${money(Math.abs(item.effect))}`}</b>{item.source === "entry" && <button onClick={() => onEditEntry(item.sourceId)} aria-label="ویرایش ثبت"><Icon name="edit" size={13} /></button>}</article>)}</div>'
new_person_list = '<div className="person-ledger-list">{account.items.map((item) => { const directSettlement = item.source === "entry" && item.entryRole === "settlement"; return <article key={item.id} className={item.status === "paid" && !directSettlement ? "ledger-item paid" : `ledger-item${directSettlement ? " settlement-entry" : ""}`}><span className={`ledger-source ${item.source}`}><Icon name={directSettlement ? "settlement" : item.source === "entry" ? "book" : item.source === "check" ? "check" : item.source === "expense" ? "receipt" : "settlement"} size={15} /></span><button className="ledger-item-open" onClick={() => onOpenItem(item)}><strong>{item.title}</strong><small>{item.detail} • {persianDate(item.date)}</small></button><b className={item.effect >= 0 ? "text-green" : "text-coral"}>{item.status === "paid" && !directSettlement ? "تسویه‌شده" : `${item.effect >= 0 ? "+" : "−"}${money(Math.abs(item.effect))}`}</b>{item.source === "entry" && item.entryRole !== "settlement" && item.status === "open" && <button onClick={() => onEditEntry(item.sourceId)} aria-label="ویرایش ثبت"><Icon name="edit" size={13} /></button>}</article>; })}</div>'
replace_once("app/FinanceApp.tsx", old_person_list, new_person_list)

old_entry_detail = '''  if (target.kind === "entry") {\n    const entry = data.entries.find((item) => item.id === target.id);\n    if (!entry) return <div className="form-empty"><Icon name="book" size={28} /><h2>این تراکنش پیدا نشد</h2><button className="submit-button" onClick={onClose}>برگشت</button></div>;\n    const category = entry.direction === "receivable" ? "طلب" : "بدهی";\n    const receipt: TransactionReceiptData = { reference: `E-${entry.id}`, category, title: entry.title, amount: entry.amount, date: persianDate(entry.createdAt.slice(0, 10), true), status: entry.status === "paid" ? "تسویه‌شده" : "باز", direction: entry.direction === "receivable" ? "positive" : "negative", fields: [{ label: "طرف حساب", value: entry.personName }, { label: "سررسید", value: entry.dueDate ? persianDate(entry.dueDate, true) : "بدون سررسید" }], note: entry.note };\n    return <TransactionDetailShell category={category} title={entry.title} amount={entry.amount} tone={entry.direction === "receivable" ? "positive" : "negative"} date={receipt.date} status={receipt.status} receipt={receipt} attachment={entry.receipt} onClose={onClose} actions={<><button onClick={() => onEditEntry(entry)}><Icon name="edit" size={14} /> ویرایش</button><button onClick={() => onToggleEntry(entry)}>{entry.status === "paid" ? "بازگردانی به باز" : "ثبت تسویه"}</button><button className="danger" onClick={() => onDeleteEntry(entry)}><Icon name="trash" size={14} /> حذف</button></>}><DetailRows rows={[["طرف حساب", entry.personName], ["نوع", category], ["تاریخ ثبت", receipt.date], ["سررسید", entry.dueDate ? persianDate(entry.dueDate, true) : "بدون سررسید"], ["وضعیت", receipt.status]]} />{entry.note && <div className="transaction-note"><small>یادداشت</small><p>{entry.note}</p></div>}</TransactionDetailShell>;\n  }'''
new_entry_detail = '''  if (target.kind === "entry") {\n    const entry = data.entries.find((item) => item.id === target.id);\n    if (!entry) return <div className="form-empty"><Icon name="book" size={28} /><h2>این تراکنش پیدا نشد</h2><button className="submit-button" onClick={onClose}>برگشت</button></div>;\n    const isSettlement = entry.role === "settlement";\n    const sourceEntry = entry.settlesEntryId ? data.entries.find((item) => item.id === entry.settlesEntryId) : undefined;\n    const settlementEntry = entry.settledByEntryId ? data.entries.find((item) => item.id === entry.settledByEntryId) : undefined;\n    const category = isSettlement ? (entry.direction === "receivable" ? "دریافت تسویه" : "پرداخت تسویه") : entry.direction === "receivable" ? "طلب" : "بدهی";\n    const status = isSettlement ? "انجام‌شده" : entry.status === "paid" ? "تسویه‌شده" : "باز";\n    const transactionDate = entry.transactionDate ?? entry.createdAt.slice(0, 10);\n    const receipt: TransactionReceiptData = { reference: `E-${entry.id}`, category, title: entry.title, amount: entry.amount, date: persianDate(transactionDate, true), status, direction: entry.direction === "receivable" ? "positive" : "negative", fields: [{ label: "طرف حساب", value: entry.personName }, ...(isSettlement && sourceEntry ? [{ label: "تسویه برای", value: sourceEntry.title }] : []), ...(!isSettlement ? [{ label: "سررسید", value: entry.dueDate ? persianDate(entry.dueDate, true) : "بدون سررسید" }] : [])], note: entry.note };\n    const relation = isSettlement ? sourceEntry : settlementEntry;\n    const relationLabel = isSettlement ? "بدهی / طلب اصلی" : "تراکنش تسویه‌کننده";\n    return <TransactionDetailShell category={category} title={entry.title} amount={entry.amount} tone={entry.direction === "receivable" ? "positive" : "negative"} date={receipt.date} status={receipt.status} receipt={receipt} attachment={entry.receipt} onClose={onClose} actions={isSettlement ? <button className="danger" onClick={() => onDeleteEntry(entry)}><Icon name="trash" size={14} /> حذف تسویه</button> : <>{entry.status === "open" && <button onClick={() => onEditEntry(entry)}><Icon name="edit" size={14} /> ویرایش</button>}<button onClick={() => onToggleEntry(entry)}>{entry.status === "paid" ? "بازگردانی به باز" : "ثبت تسویه"}</button><button className="danger" onClick={() => onDeleteEntry(entry)}><Icon name="trash" size={14} /> حذف</button></>}><DetailRows rows={[["طرف حساب", entry.personName], ["نوع", category], [isSettlement ? "تاریخ تسویه" : "تاریخ ثبت", receipt.date], ...(!isSettlement ? [["سررسید", entry.dueDate ? persianDate(entry.dueDate, true) : "بدون سررسید"] as [string, string]] : []), ["وضعیت", receipt.status]]} />{relation && <section className="detail-payment-list"><h3>{relationLabel}</h3><button onClick={() => onOpenDetail({ kind: "entry", id: relation.id })}><span><strong>{relation.title}</strong><small>{relation.role === "settlement" ? "پرداخت/دریافت ثبت‌شده برای بستن این حساب" : `${relation.direction === "receivable" ? "طلب" : "بدهی"} اصلی`}</small></span><b>{money(relation.amount)} ‹</b></button></section>}{entry.note && <div className="transaction-note"><small>یادداشت</small><p>{entry.note}</p></div>}</TransactionDetailShell>;\n  }'''
replace_once("app/FinanceApp.tsx", old_entry_detail, new_entry_detail)

# tests and cache
p = Path("public/sw.js")
sw = p.read_text(encoding="utf-8").replace('daftar-hesab-offline-v14', 'daftar-hesab-offline-v15')
p.write_text(sw, encoding="utf-8")

p = Path("tests/rendered-html.test.mjs")
tests = p.read_text(encoding="utf-8")
tests = tests.replace('/daftar-hesab-offline-v14/', '/daftar-hesab-offline-v15/')
tests += '''\n\ntest("direct debt and receivable settlements create linked counterpart transactions", async () => {\n  const [app, localDb, serviceWorker] = await Promise.all([\n    readFile(new URL("app/FinanceApp.tsx", root), "utf8"),\n    readFile(new URL("app/local-db.ts", root), "utf8"),\n    readFile(new URL("public/sw.js", root), "utf8"),\n  ]);\n  assert.match(localDb, /role: "obligation" \| "settlement"/);\n  assert.match(localDb, /settlesEntryId/);\n  assert.match(localDb, /settledByEntryId/);\n  assert.match(localDb, /operation === "settle_entry"/);\n  assert.match(localDb, /kind: "settlement"/);\n  assert.match(localDb, /operation === "reopen_entry"/);\n  assert.match(localDb, /item\.settlesEntryId === id/);\n  assert.match(app, /DirectEntrySettlementForm/);\n  assert.match(app, /ثبت تراکنش تسویه/);\n  assert.match(app, /تراکنش تسویه‌کننده/);\n  assert.match(app, /بدهی \/ طلب اصلی/);\n  assert.match(app, /onToggleEntry=\{handleEntrySettlement\}/);\n  assert.doesNotMatch(app, /onToggleEntry=\{\(entry\) => void post\(\{ operation: "toggle_entry"/);\n  assert.match(serviceWorker, /daftar-hesab-offline-v15/);\n});\n'''
p.write_text(tests, encoding="utf-8")
